from setuptools import setup

setup(
   name='unimatch',
   version='1.0',
   description='Unimatch optical flow',
   author='Man Foo',
   author_email='felix.wimbauer@tum.de',
   packages=['unimatch'],  #same as name
   install_requires=[], #external packages as dependencies
)
